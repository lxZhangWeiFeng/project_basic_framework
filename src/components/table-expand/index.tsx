import React, { FC } from 'react'
// import styles from './index.less'

const TableExpand: FC = ({ children }) => {
    return <>{children}</>
    // return <div className={styles['expand']}>{children}</div>
}

export default TableExpand
